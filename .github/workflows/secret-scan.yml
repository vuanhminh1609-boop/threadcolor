name: QuÃ©t bÃ­ máº­t

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  secret_scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Kiểm tra file ignore tồn tại
        run: |
          test -f .gitleaksignore
          sed -n '1,200p' .gitleaksignore
      - name: Quét bí mật bằng gitleaks
        uses: gitleaks/gitleaks-action@v2
        with:
          args: >
            detect --gitleaks-ignore-path=.gitleaksignore
